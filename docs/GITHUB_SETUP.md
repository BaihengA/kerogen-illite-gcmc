# GitHub Setup

Target repository suggestion: `BaihengA/kerogen-md-gcmc-workflows`.

## Probe Results

### gh auth status
```text
[WinError 2] 系统找不到指定的文件。
```
### git remote -v
```text
no remote configured
```

If the repository is not found or gh is unavailable, status is `REPO_NOT_FOUND_OR_GH_UNAVAILABLE`; no upload is claimed.
